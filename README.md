## Getting Started

First, run the development server:

```bash
npm run dev

```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

mongodb, express, react, node, nextjs, tailwindcss, docker, docker-compose, github actions, vercel, swagger, nodemailer, python, flask, fastapi, postgres, redis, azure, netlify, firebase, stripe, twilio, cloudinary rabbitmq, kafka, kubernetes, gitlab, bitbucket, jira, trello, notion,

tools which developer use
- github
- figma
- notion
- docker
- postman
- vscode
- wordpress

